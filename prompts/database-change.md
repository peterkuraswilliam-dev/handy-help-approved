# Prompt: Database change

```text
Plan and implement the requested Handy Help database change only after reading the owning feature spec, database docs, RLS matrix and actual migrations/schema. State existing vs proposed objects. Include migration, constraints, indexes, timestamps, ownership, status transitions, public-safe projection, RLS policies, Storage policies if relevant, backfill, generated types, tests, rollback/mitigation and documentation updates.

Never disable or weaken RLS. Never expose service-role credentials. Stop for a product decision if ownership, visibility, retention or transition authority is unclear.
```
